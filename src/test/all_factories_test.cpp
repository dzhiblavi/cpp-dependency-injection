#include <gtest/gtest.h>
#include <google/protobuf/util/json_util.h>

#include "src/test/classes.hpp"

//TEST(all_factories, int_factory) {
  //IFactory factory;

  //std::unique_ptr<I> i(factory.Create(1010));
  //EXPECT_EQ(1010, i->Value());
//}


//TEST(all_factories, protobuf_factory) {
  //IFactory factory;
  //IProto iproto;
  //google::protobuf::util::JsonStringToMessage(i_json_config, &iproto);

  //std::unique_ptr<I> i(factory.Create(iproto));
  //EXPECT_EQ(iproto.dproto().x(), i->Value());
//}

